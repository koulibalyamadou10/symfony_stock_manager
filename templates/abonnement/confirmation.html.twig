{% extends 'base.html.twig' %}

{% block title %}Confirmation de paiement{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header bg-success text-white text-center">
                    <h4 class="mb-0">
                        <i class="fas fa-check-circle me-2"></i>
                        Paiement en cours de traitement
                    </h4>
                </div>
                <div class="card-body text-center">
                    <div class="mb-4">
                        <i class="fas fa-clock text-warning" style="font-size: 4rem;"></i>
                    </div>
                    
                    <h5>Merci pour votre paiement !</h5>
                    <p class="text-muted">
                        Votre paiement est en cours de traitement. Vous recevrez une confirmation par email 
                        dès que votre abonnement sera activé.
                    </p>
                    
                    <div class="alert alert-info">
                        <small>
                            <i class="fas fa-info-circle me-2"></i>
                            Le traitement peut prendre quelques minutes. Vous pouvez vérifier le statut 
                            de votre abonnement à tout moment.
                        </small>
                    </div>
                    
                    <div class="d-grid gap-2">
                        <a href="{{ path('app_abonnement_status') }}" class="btn btn-primary">
                            <i class="fas fa-eye me-2"></i>
                            Vérifier le statut
                        </a>
                        <a href="{{ path('app_dashboard') }}" class="btn btn-outline-secondary">
                            <i class="fas fa-home me-2"></i>
                            Retour à l'accueil
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
// Auto-refresh pour vérifier le statut de l'abonnement
setTimeout(function() {
    window.location.href = "{{ path('app_abonnement_status') }}";
}, 30000); // Redirection après 30 secondes
</script>
{% endblock %}
